/* Models for reviews and comments */

class Review {
    static getReview(id) {
        let sql = `SELECT review_id AS 'reviewId', first`
    }
}

class Comment {

}

module.exports = { Review, Comment };